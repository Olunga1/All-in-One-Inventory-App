# Optional: use an external data directory instead of the local .data folder.
# Keep this path on persistent storage in production.
# DATA_DIR=/var/lib/stockroom

# Optional: hide public demo sign-in in a production deployment.
# DISABLE_DEMO=true

# The Express server listens on PORT (or API_PORT in local development).
# PORT=4174
